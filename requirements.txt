# DAINet — Direction-Aware Illumination Network
# Python 3.10. Tested with the CUDA 12.1 PyTorch wheels on a single 24 GB GPU.
# Install PyTorch first with the matching CUDA build, e.g.:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# then:  pip install -r requirements.txt  &&  pip install -e .

# --- core model / training ---
torch==2.5.1
torchvision==0.20.1
timm==1.0.27
numpy==1.26.4
scipy==1.12.0
opencv-python==4.9.0.80
pillow==12.2.0
pyyaml==6.0.3
tqdm==4.67.3
matplotlib==3.10.9

# --- metrics / evaluation ---
lpips==0.1.4
scikit-image==0.25.2
scikit-learn==1.7.2
pandas==2.3.3
pyiqa==0.1.15.post2      # no-reference metrics (NIQE / BRISQUE) in the benchmark harness

# --- experiment logging ---
wandb==0.26.1

# --- prior precomputation (surface normals) ---
geffnet==1.0.2           # DSINE backbone
diffusers==0.38.0        # Marigold normals fallback

# --- tests ---
pytest==9.0.2

# --- installed from source, NOT pip (see README "Installation") ---
# SAM2  (segment-anything-2)  -> multi-view segmentation priors (scripts/precompute_sam.py, data/segmentation.py)
# DSINE                       -> surface normals (scripts/precompute_normals.py)
